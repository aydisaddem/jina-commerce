[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA fallback for React Router - this is all you need
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200